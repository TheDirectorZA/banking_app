spring:
  application:
    name: banking-api

  datasource:
    url: jdbc:postgresql://localhost:5432/catalyst_core
    username: catalyst_user
    password: catalyst123

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
